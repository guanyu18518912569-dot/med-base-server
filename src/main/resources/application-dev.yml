server:
  port: 8889
spring:
  datasource:
    url: jdbc:mysql://39.100.83.107:3326/small_mall?characterEncoding=UTF-8&&zeroDateTimeBehavior=convertToNull&autoReconnect=true&failOverReadOnly=false&connectTimeout=0&useSSL=true&queryTimeout=2400&serverTimezone=Asia/Shanghai
    username: root
    password: 5tgbnhy6%TGBNHY^
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      auto-commit: true
      idle-timeout: 10000
      pool-name: DataSourceHk
      max-lifetime: 1800000
      connection-timeout: 30000
      maximum-pool-size: 20
      connection-test-query: SELECT 1
  mvc:
    favicon:
      enabled: false
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
logging:
  level:
    com.*: debug
  config: classpath:logback-spring.xml
mybatis-plus:
  type-aliases-package: med.base.server.model
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

qiniu:
  # 请在本地开发环境中配置自己的七牛 AccessKey / SecretKey
  access-key: yc8WqvjKlPqdrTQCnl7L6I0ODA7UA-cNPx6TSGHS
  secret-key: 5WDEvkMfqqdSp2Wi44pBIthSGiyMKOiPeOPcZFAW
  # 空间名称
  bucket: swzx-samll-mall
  # 访问域名前缀（不带协议）
  domain: static.siweizx.com
